
1、简化默认行为函数，以包含的方式添加行为的支持

2、数据岛放弃采用低效率的path存储结构，不再跟当前path绑定，影响插件依赖域，直接采用变量命名存储，添加变量域的支持，（全局，当前包）

3、简化、整理ajax、dom操作和_的代码

4、mediator放弃使用path的存储结构，添加订阅域的支持，（全局，当前包）

5、裁剪backbone的路由解析机制

6、util添加自动初始化和反初始化的支持

7、测试继承机制

8、添加特殊事件支持: 	启动加载前[app]，(加载中N)处理前-处理后[app,widget]，加载完（初始化前）[app,widget]
			（初始化时N)初始化[widget]，初始化完毕[app,widget]

9、整合函数。

10、执行队列控制queue.add('default.mark',func,200),可实现多队列控制，实现时间线的控制，可abort某些执行，例如可防止多次ajax异步请求，检测是否有类似或相同的行为在等待进行。

11、dom清理机制，插件实现定期清理隐藏结构，用到时再加载。

12、包组装，以包为对象进行组合形成新的包，拥有同包访问权限。

13、ajax需要支持标签功能，以检测是否有相同类型请求正在进行








requirejs
require一个文件，直接对象下载后callback马上执行，直接对象的callback等待其他依赖对象下载后再执行


















